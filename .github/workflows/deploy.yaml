name: Deploy MultiDocker
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
      - run: docker build -t jrico/react-test -f ./client/Dockerfile.dev ./client
      - run: docker run -e CI=true jrico/react-test npm test

      - run: docker build -t jrico/multi-client ./client
      - run: docker build -t jrico/multi-nginx ./nginx
      - run: docker build -t jrico/multi-server ./server
      - run: docker build -t jrico/multi-worker ./worker

      - run: docker push jrico/multi-client
      - run: docker push jrico/multi-nginx
      - run: docker push jrico/multi-server
      - run: docker push jrico/multi-worker
